[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "physical-reservoir"
version = "0.1.0"
description = "Differentiable physical reservoir computing experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "jax",
  "jaxlib",
  "equinox",
  "diffrax",
  "jaxtyping",
  "optax",
  "scikit-learn",
  "matplotlib",
  "pytest"
]

[tool.setuptools.packages.find]
include = ["physres*"]
